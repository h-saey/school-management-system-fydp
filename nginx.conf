# server {
#     listen 80;

#     # React frontend
#     location / {
#         root /usr/share/nginx/html;
#         index index.html;
#         try_files $uri /index.html;
#     }

#     # Backend API
#     location /api/ {
#         proxy_pass http://sms-api:8080/;
#     }
# }
server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }

    location /api/ {
        proxy_pass http://sms-api:8080;
    }
}